{
  "name": "backslash",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "jest",
    "db-handler:deploy": "tsc db-handler/index.ts &&  cd db-handler && zip -r db-handler.zip index.js node_modules/sqlstring && cd .. && aws lambda update-function-code --function-name db-handler --region eu-central-1 --zip-file fileb://db-handler/db-handler.zip"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@types/google-one-tap": "^1.2.0",
    "@types/google.accounts": "^0.0.2",
    "@vitejs/plugin-react": "^1.3.0",
    "aws-sdk": "^2.1167.0",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.1",
    "typescript": "^4.6.3",
    "vite": "^2.9.9"
  }
}
